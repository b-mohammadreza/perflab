[package]
name = "perflab"
version = "0.0.0"
edition = "2024"
description = "'Compiler-to-Perf Lab' for MLIR/LLVM (CPU + GPU)"

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.53", features = ["derive"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.148"
